# PhycoBot

Telegram-бот для проведения психологических опросов.

## Описание

Бот проводит последовательные опросы среди пользователей Telegram. Ответы сохраняются в SQLite, каждое прохождение учитывается отдельно (`run_id`). После прохождения всех вопросов доступны кнопки: перезапуск опроса, просмотр статистики и сброс данных (с подтверждением).

## Возможности

- 📋 Последовательное прохождение опросов (опросы берутся из БД)
- 📊 Статистика по всем прохождениям (команда `/stats` и кнопка «Статистика»)
- 🔄 Кнопка **«Запустить снова»** — перезапуск опроса с первого вопроса (после завершения и после просмотра статистики)
- 🗑 Кнопка **«Сброс данных»** — обнуление статистики с подтверждением («Вы уверены?» → Да / Нет), при «Нет» — возврат к предыдущему экрану
- 💾 Хранение в SQLite (`bot.db`)
- 🛑 Корректное завершение по Ctrl+C (graceful shutdown)

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/mxchgeCode/PhycoBot.git
   cd PhycoBot
   ```

2. Создайте виртуальное окружение и установите зависимости:
   ```bash
   python -m venv .venv
   .venv\Scripts\activate   # Windows
   # source .venv/bin/activate  # Linux/macOS
   pip install -r requirements.txt
   ```

3. Настройте токен бота:
   - Создайте файл `.env` в корне проекта и добавьте:
     ```
     BOT_TOKEN=ваш_токен_от_BotFather
     ```
   - Либо задайте переменную окружения `BOT_TOKEN`.

4. При первом запуске, если в БД нет опросов, создаются примеры (см. `main.py`). Свои опросы можно добавить через функцию `add_poll()` или напрямую в таблицу `polls`.

## Запуск

```bash
python main.py
```

Остановка: **Ctrl+C** — бот корректно завершает работу.

## Команды

| Команда / действие   | Описание                                    |
|----------------------|---------------------------------------------|
| `/start`             | Начать прохождение опросов                  |
| `/stats`             | Показать статистику по всем опросам         |
| Сброс данных (кнопка)| Обнулить статистику (появляется подтверждение «Да» / «Нет») |

## Интерфейс после прохождения опроса

- **Опрос пройден успешно!** — кнопки: «Запустить снова», «Статистика», «Сброс данных».
- После нажатия **«Статистика»** — текст статистики и кнопки: «Запустить снова», «Сброс данных».
- **«Сброс данных»** — вопрос «Вы уверены? Это действие удалит данные статистики.» и кнопки «Да» / «Нет». При «Да» выполняется сброс, при «Нет» — возврат к экрану «Опрос пройден» или к экрану статистики.

## База данных

SQLite, файл `bot.db`.

- **polls** — опросы: `id`, `question`, `options` (варианты через `|||`), `poll_type`.
- **answers** — ответы: `poll_id`, `user_id`, `option_index`, `run_id` (номер прохождения).

## Зависимости

Указаны в `requirements.txt`, в том числе:

- `python-telegram-bot` — работа с Telegram Bot API
- `python-dotenv` — загрузка переменных из `.env`

## Лицензия

MIT
